其中有两个#cgo命令，分别是编译和链接参数。CFLAGS通过-I./将库对应头文件所在的目录加入头文件检索路径。LDFLAGS通过-L${SRCDIR}/将编译后静态库所在目录加为链接库检索路径，-lbuffer表示链接libbuffer.a静态库。需要注意的是，在链接部分的检索路径不能使用相对路径（C/C++代码的链接程序所限制），我们必须通过cgo特有的${SRCDIR}变量将源文件对应的当前目录路径展开为绝对路径（因此在windows平台中绝对路径不能有空白符号）。


